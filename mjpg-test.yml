mjpg-streamer:
  restart: always
  image: kvaps/mjpg-streamer
  devices:
    - /dev/video0
  ports:
    - 8090:8090
  command: -i "/usr/lib64/input_uvc.so -d /dev/video0 -r 1280x720 -f 30" -o "/usr/lib64/output_http.so -p 8090 -w /usr/share/mjpeg-streamer/www/ -c admin:admin"